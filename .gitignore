# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Firebase generated files (contains sensitive API keys)
lib/firebase_options.dart
lib/config/firebase_options.dart

# App configuration files (contains sensitive AdMob IDs and API keys)
lib/config/app_config.dart

# Google Services configuration files (contains sensitive API keys)
# Android
android/app/google-services.json
**/google-services.json

# iOS
ios/Runner/GoogleService-Info.plist
**/GoogleService-Info.plist

# Web
web/firebase-config.js
web/firebase-messaging-sw.js
**/firebase-config.js
**/firebase-messaging-sw.js

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# ============================================
# SECURITY: Sensitive Data & Credentials
# ============================================

# Environment variables and secrets
.env
.env.local
.env.*.local
.env.production
.env.development
*.env
secrets.json
config.json
credentials.json
api-keys.json

# Android signing keys and certificates (CRITICAL - Never commit!)
android/key.properties
android/app/keystore.properties
*.jks
*.keystore
*.key
*.p12
*.pfx
android/app/release.keystore
android/app/debug.keystore
**/keystore/
**/keys/

# iOS certificates and provisioning profiles (CRITICAL - Never commit!)
ios/*.mobileprovision
ios/*.provisionprofile
ios/*.certSigningRequest
ios/*.p12
ios/*.cer
ios/*.cert
ios/DistributionSummary.plist
ios/ExportOptions.plist
ios/Runner.xcarchive
*.xcuserdata
ios/Pods/
ios/.symlinks/

# API keys and tokens
**/api_keys.dart
**/secrets.dart
**/credentials.dart
**/*_secret.*
**/*_key.*
**/*_token.*

# Database files (may contain user data)
*.db
*.sqlite
*.sqlite3
*.db-shm
*.db-wal
**/*.db
**/*.sqlite

# Log files (may contain sensitive data)
*.log
logs/
**/logs/
*.log.*
crash.log
error.log
debug.log

# Backup files (may contain sensitive data)
*.bak
*.backup
*.old
*.tmp
*~
.#*
\#*#

# Crash reports and analytics data
crash_reports/
analytics/
**/crashlytics/
**/firebase_crashlytics/

# Local configuration overrides
local.properties
local_config.dart
local_settings.json

# IDE and editor files (may contain paths or settings)
.vscode/settings.json
.vscode/launch.json
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries/
.idea/shelf/
*.swp
*.swo
*~

# Temporary and cache files
tmp/
temp/
cache/
.cache/
*.cache

# Package manager lock files (optional - some teams ignore these)
# pubspec.lock  # Uncomment if you want to ignore pubspec.lock

# Build outputs (may contain embedded keys)
build/
dist/
out/
*.apk
*.aab
*.ipa
*.app
*.dSYM.zip

# Test coverage (may contain code paths)
coverage/
.nyc_output/
*.lcov

# Documentation that might contain sensitive info
docs/secrets.md
docs/credentials.md
**/SECRETS.md
**/CREDENTIALS.md
